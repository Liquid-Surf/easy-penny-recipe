{
  "scripts": {
    "start": "npx community-solid-server -c config.json -f data",
    "start:docker": "npx community-solid-server -c config.json -f /data -p 3056"
  },
  "dependencies": {
    "@solid/community-server": "^7.0.4",
    "easy-penny-pod-inspector": "^0.1.2"
  }
}
